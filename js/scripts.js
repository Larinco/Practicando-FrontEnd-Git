

import {animateHeaderTitle} from "./animateHeader.js"

const header__title  = document.getElementById("header__title");

document.addEventListener("DOMContentLoaded", ()=>{
    animateHeaderTitle(header__title)
})